#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

void insertionSort(vector<int>& arr) {
    int i=1; 
    int n=arr.size();
    for(i; i<n; i+=2) {
        lower_bound(arr.begin(), arr.begin()+i+1, )
    }

}

int main() {
    return 0;
}